import { Injectable, NotFoundException } from '@nestjs/common'; import { PrismaService } from '../../database/prisma.service'; import type { CreateHeroSlideDto,CreatePartnerDto,UpdateHeroSlideDto,UpdatePartnerDto } from './homepage.dto';
@Injectable() export class HomepageService { constructor(private readonly prisma:PrismaService){} slides(admin=false){return this.prisma.heroSlide.findMany({where:admin?{}:{isActive:true},orderBy:[{sortOrder:'asc'},{createdAt:'asc'}]})} partners(admin=false){return this.prisma.strategicPartner.findMany({where:admin?{}:{isActive:true},orderBy:[{sortOrder:'asc'},{createdAt:'asc'}]})} createSlide(data:CreateHeroSlideDto){return this.prisma.heroSlide.create({data})} createPartner(data:CreatePartnerDto){return this.prisma.strategicPartner.create({data})} async updateSlide(id:string,data:UpdateHeroSlideDto){await this.requireSlide(id);return this.prisma.heroSlide.update({where:{id},data})} async updatePartner(id:string,data:UpdatePartnerDto){await this.requirePartner(id);return this.prisma.strategicPartner.update({where:{id},data})} async deleteSlide(id:string){await this.requireSlide(id);await this.prisma.heroSlide.delete({where:{id}})} async deletePartner(id:string){await this.requirePartner(id);await this.prisma.strategicPartner.delete({where:{id}})} private async requireSlide(id:string){if(!await this.prisma.heroSlide.findUnique({where:{id},select:{id:true}}))throw new NotFoundException('Slide not found')} private async requirePartner(id:string){if(!await this.prisma.strategicPartner.findUnique({where:{id},select:{id:true}}))throw new NotFoundException('Partner not found')} }
